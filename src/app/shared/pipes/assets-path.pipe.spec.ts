import { AssetsPathPipe } from './assets-path.pipe';

describe('AssetsPathPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsPathPipe();
    expect(pipe).toBeTruthy();
  });
});
